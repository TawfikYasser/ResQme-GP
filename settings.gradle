pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter()
        maven {
            url  "https://cardinalcommerceprod.jfrog.io/ui/native/android"
            credentials{
                username usernameP
                password passwordP
            }
        }
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "ResQme"
include ':app'
